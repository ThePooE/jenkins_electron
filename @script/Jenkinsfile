#!groovy
node {
    stage ('Download') {
        checkout scm
        echo 'Yay!'
    }
    stage ('Setup') {
        bat 'npm run package-properties'
        echo ${env.BUILD_ID}
        bat 'npm install'
    }
}